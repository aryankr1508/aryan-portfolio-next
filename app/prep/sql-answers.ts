// Generated from c:/Users/aryan/Downloads/sql-interview-guide.md

export const sqlAnswers: string[] = [
  "### Theory\n\nBoth filter data, but at different stages of query execution.\n\n**WHERE** filters individual **rows** BEFORE grouping happens. It works on raw data.\n\n**HAVING** filters **groups** AFTER GROUP BY has aggregated the data. It works on aggregated results like SUM, COUNT, AVG.\n\n**SQL execution order - this is the key:**\n```\n1. FROM       - pick the table\n2. WHERE      - filter individual rows\n3. GROUP BY   - group remaining rows\n4. HAVING     - filter the groups\n5. SELECT     - pick columns to show\n6. ORDER BY   - sort results\n```\n\n```sql\n-- WHERE: \"only look at completed orders\"\n-- HAVING: \"only show customers who spent over $5000\"\n\nSELECT CustomerId, COUNT(*) AS OrderCount, SUM(Amount) AS TotalSpent\nFROM Orders\nWHERE Status = 'Completed'           -- filters ROWS before grouping\nGROUP BY CustomerId\nHAVING SUM(Amount) > 5000            -- filters GROUPS after aggregation\nORDER BY TotalSpent DESC;\n```\n\n**Why can't WHERE do HAVING's job?**\n```sql\n-- THIS BREAKS - WHERE runs before GROUP BY, so COUNT doesn't exist yet\nWHERE COUNT(*) > 5   -- ERROR!\n\n-- THIS WORKS - HAVING runs after GROUP BY\nHAVING COUNT(*) > 5  -- [ok]\n```\n\n### Interview Answer\n\n> \"WHERE filters individual rows before any grouping - like removing cancelled orders before aggregating. HAVING filters after GROUP BY, so it works on aggregate results like SUM, COUNT, AVG. I always push as much filtering into WHERE as possible because it reduces the number of rows GROUP BY has to process, which is better for performance.\"",
  "### Theory\n\nJOINs combine rows from two or more tables based on a related column.\n\n**Setup for all examples:**\n```\nCustomers:                    Orders:\n| Id | Name    |             | Id | CustomerId | Amount |\n|----|---------|             |----|------------|--------|\n| 1  | Alice   |             | 10 | 1          | 500    |\n| 2  | Bob     |             | 11 | 1          | 300    |\n| 3  | Charlie |             | 12 | 2          | 700    |\n|    |         |             | 13 | 99         | 100    |\n\nNote: Charlie has no orders. Order 13 has CustomerId 99 (no matching customer).\n```\n\n### INNER JOIN - Only matching rows from both sides\n\n```sql\nSELECT c.Name, o.Amount\nFROM Customers c\nINNER JOIN Orders o ON c.Id = o.CustomerId;\n\n-- Result:\n-- Alice  | 500\n-- Alice  | 300\n-- Bob    | 700\n-- Charlie is EXCLUDED (no orders)\n-- Order 13 is EXCLUDED (no matching customer)\n```\n\nThink: **intersection** - only rows that have a match in BOTH tables.\n\n### LEFT JOIN - All rows from left + matching from right\n\n```sql\nSELECT c.Name, o.Amount\nFROM Customers c\nLEFT JOIN Orders o ON c.Id = o.CustomerId;\n\n-- Result:\n-- Alice   | 500\n-- Alice   | 300\n-- Bob     | 700\n-- Charlie | NULL    <- included even with no orders\n-- Order 13 still excluded (not in left table)\n```\n\nThink: **everything from the left table**, nulls where right has no match.\n\n### RIGHT JOIN - All rows from right + matching from left\n\n```sql\nSELECT c.Name, o.Amount\nFROM Customers c\nRIGHT JOIN Orders o ON c.Id = o.CustomerId;\n\n-- Result:\n-- Alice | 500\n-- Alice | 300\n-- Bob   | 700\n-- NULL  | 100    <- Order 13 included, no matching customer\n-- Charlie excluded (no orders)\n```\n\nThink: **everything from the right table**, nulls where left has no match.\n\n### FULL OUTER JOIN - Everything from both sides\n\n```sql\nSELECT c.Name, o.Amount\nFROM Customers c\nFULL OUTER JOIN Orders o ON c.Id = o.CustomerId;\n\n-- Result:\n-- Alice   | 500\n-- Alice   | 300\n-- Bob     | 700\n-- Charlie | NULL   <- no orders\n-- NULL    | 100    <- no matching customer\n```\n\nThink: **union of both sides** - nobody is excluded.\n\n### CROSS JOIN - Every row paired with every other row\n\n```sql\nSELECT c.Name, o.Amount\nFROM Customers c\nCROSS JOIN Orders o;\n\n-- Result: 3 customers x 4 orders = 12 rows\n-- Alice   | 500, Alice   | 300, Alice   | 700, Alice   | 100\n-- Bob     | 500, Bob     | 300, Bob     | 700, Bob     | 100\n-- Charlie | 500, Charlie | 300, Charlie | 700, Charlie | 100\n```\n\nThink: **cartesian product** - every possible combination. Rarely useful except for generating test data or date ranges.\n\n### SELF JOIN - Table joined with itself\n\n```sql\n-- Employee-Manager hierarchy\n-- Employees:\n-- | Id | Name  | ManagerId |\n-- | 1  | Alice | NULL      |  (CEO, no manager)\n-- | 2  | Bob   | 1         |  (reports to Alice)\n-- | 3  | Carol | 1         |  (reports to Alice)\n-- | 4  | Dave  | 2         |  (reports to Bob)\n\nSELECT e.Name AS Employee, m.Name AS Manager\nFROM Employees e\nLEFT JOIN Employees m ON e.ManagerId = m.Id;\n\n-- Result:\n-- Alice | NULL   (no manager)\n-- Bob   | Alice\n-- Carol | Alice\n-- Dave  | Bob\n```\n\nThink: **the same table used twice** with different aliases.\n\n### Interview Answer\n\n> \"INNER JOIN returns only matching rows from both tables. LEFT JOIN returns all rows from the left plus matching from the right, with NULLs for no match. RIGHT JOIN is the reverse. FULL OUTER returns everything from both sides. CROSS JOIN produces the cartesian product - every combination. SELF JOIN joins a table to itself - I use it for hierarchical data like employee-manager relationships.\n>\n> In practice, I use INNER JOIN when I only want matched data, and LEFT JOIN when I need all records from one side even if the other side has no match - like listing all customers including those with no orders.\"",
  "### Theory\n\nBoth combine results from multiple SELECT statements into one result set.\n\n```sql\n-- Table A: Alice, Bob\n-- Table B: Bob, Charlie\n\n-- UNION - removes duplicates\nSELECT Name FROM TableA\nUNION\nSELECT Name FROM TableB;\n-- Result: Alice, Bob, Charlie (Bob appears once)\n\n-- UNION ALL - keeps duplicates\nSELECT Name FROM TableA\nUNION ALL\nSELECT Name FROM TableB;\n-- Result: Alice, Bob, Bob, Charlie (Bob appears twice)\n```\n\n**UNION ALL is faster** because it doesn't need to sort and deduplicate. Use it when you know there are no duplicates or when duplicates are acceptable.\n\n**Rules:** Both SELECTs must have the same number of columns with compatible data types.\n\n### Interview Answer\n\n> \"UNION combines results and removes duplicates, which requires a sort operation. UNION ALL keeps everything including duplicates, so it's faster. I default to UNION ALL when I know the sources don't overlap or when duplicates are acceptable - like combining active and archived orders from separate tables.\"",
  "### Theory\n\nAggregate functions compute a single value from a set of rows. GROUP BY tells SQL which rows to aggregate together.\n\n```sql\n-- Sample data:\n-- Orders: | Department | Employee | Amount |\n--         | Sales      | Alice    | 500    |\n--         | Sales      | Alice    | 300    |\n--         | Sales      | Bob      | 700    |\n--         | Support    | Carol    | 200    |\n--         | Support    | Dave     | 400    |\n\n-- COUNT - how many rows\nSELECT Department, COUNT(*) AS OrderCount\nFROM Orders GROUP BY Department;\n-- Sales: 3, Support: 2\n\n-- SUM - total\nSELECT Department, SUM(Amount) AS Total\nFROM Orders GROUP BY Department;\n-- Sales: 1500, Support: 600\n\n-- AVG - average\nSELECT Department, AVG(Amount) AS AvgAmount\nFROM Orders GROUP BY Department;\n-- Sales: 500, Support: 300\n\n-- MIN / MAX\nSELECT Department, MIN(Amount) AS Smallest, MAX(Amount) AS Largest\nFROM Orders GROUP BY Department;\n-- Sales: 300/700, Support: 200/400\n\n-- COMBINED\nSELECT\n    Department,\n    COUNT(*)        AS OrderCount,\n    SUM(Amount)     AS Total,\n    AVG(Amount)     AS Average,\n    MIN(Amount)     AS Smallest,\n    MAX(Amount)     AS Largest\nFROM Orders\nGROUP BY Department;\n```\n\n**Rule:** Every column in SELECT that isn't an aggregate MUST be in GROUP BY.\n\n```sql\n-- WRONG - Employee is not in GROUP BY and not aggregated\nSELECT Department, Employee, SUM(Amount)\nFROM Orders\nGROUP BY Department;  -- ERROR\n\n-- RIGHT - either add Employee to GROUP BY or aggregate it\nSELECT Department, COUNT(DISTINCT Employee) AS EmployeeCount, SUM(Amount)\nFROM Orders\nGROUP BY Department;\n```\n\n### Interview Answer\n\n> \"Aggregate functions - SUM, COUNT, AVG, MIN, MAX - collapse multiple rows into one value. GROUP BY defines the grouping. Every non-aggregated column in SELECT must be in GROUP BY. I combine these with HAVING to filter groups - like 'departments with more than 5 employees' or 'customers who spent over $10,000'.\"",
  "### Theory\n\nA subquery is a query inside another query.\n\n**Non-correlated subquery** - runs ONCE, independently. The inner query doesn't reference the outer query.\n\n```sql\n-- \"Find employees who earn more than the company average\"\nSELECT Name, Salary\nFROM Employees\nWHERE Salary > (SELECT AVG(Salary) FROM Employees);\n--               ^ this runs ONCE, gets the average, then outer query uses it\n```\n\n**Correlated subquery** - runs ONCE PER ROW of the outer query. The inner query references the outer query.\n\n```sql\n-- \"Find employees who earn more than their DEPARTMENT's average\"\nSELECT e.Name, e.Salary, e.Department\nFROM Employees e\nWHERE e.Salary > (\n    SELECT AVG(e2.Salary)\n    FROM Employees e2\n    WHERE e2.Department = e.Department  -- <- references outer query!\n);\n-- For EACH employee, it calculates that employee's department average\n-- 100 employees = 100 times the subquery runs. Can be slow.\n```\n\n**Performance:** Non-correlated is usually faster (runs once). Correlated can be slow on large tables because it runs for every row. Often rewritable as a JOIN.\n\n```sql\n-- Same result, better performance with JOIN\nSELECT e.Name, e.Salary, e.Department\nFROM Employees e\nJOIN (\n    SELECT Department, AVG(Salary) AS AvgSal\n    FROM Employees\n    GROUP BY Department\n) dept_avg ON e.Department = dept_avg.Department\nWHERE e.Salary > dept_avg.AvgSal;\n```\n\n### Interview Answer\n\n> \"Non-correlated subqueries run once independently - like getting the company average salary. Correlated subqueries reference the outer query and run once per row - like comparing each employee against their department's average. Correlated can be slow on large tables because they execute repeatedly. I often rewrite them as JOINs or CTEs for better performance.\"",
  "### Theory\n\nA CTE is a temporary named result set that exists only for the duration of the query. It makes complex queries readable by breaking them into steps.\n\n```sql\n-- WITHOUT CTE - nested, hard to read\nSELECT * FROM (\n    SELECT Department, AVG(Salary) AS AvgSal\n    FROM Employees GROUP BY Department\n) dept_avg\nJOIN Employees e ON e.Department = dept_avg.Department\nWHERE e.Salary > dept_avg.AvgSal;\n\n-- WITH CTE - step by step, readable\nWITH DeptAverage AS (\n    SELECT Department, AVG(Salary) AS AvgSal\n    FROM Employees\n    GROUP BY Department\n)\nSELECT e.Name, e.Salary, d.AvgSal\nFROM Employees e\nJOIN DeptAverage d ON e.Department = d.Department\nWHERE e.Salary > d.AvgSal;\n```\n\n**Multiple CTEs chained:**\n```sql\nWITH\nActiveCustomers AS (\n    SELECT Id, Name FROM Customers WHERE IsActive = 1\n),\nRecentOrders AS (\n    SELECT CustomerId, SUM(Amount) AS Total\n    FROM Orders\n    WHERE OrderDate > DATEADD(MONTH, -3, GETDATE())\n    GROUP BY CustomerId\n)\nSELECT ac.Name, ro.Total\nFROM ActiveCustomers ac\nJOIN RecentOrders ro ON ac.Id = ro.CustomerId\nORDER BY ro.Total DESC;\n```\n\n**Recursive CTE - for hierarchical data:**\n```sql\n-- Employee hierarchy: who reports to whom, all levels deep\nWITH OrgChart AS (\n    -- Base: start with the CEO (no manager)\n    SELECT Id, Name, ManagerId, 0 AS Level\n    FROM Employees WHERE ManagerId IS NULL\n\n    UNION ALL\n\n    -- Recursive: find everyone who reports to the previous level\n    SELECT e.Id, e.Name, e.ManagerId, oc.Level + 1\n    FROM Employees e\n    JOIN OrgChart oc ON e.ManagerId = oc.Id\n)\nSELECT * FROM OrgChart ORDER BY Level;\n-- Level 0: CEO\n-- Level 1: VPs who report to CEO\n-- Level 2: Directors who report to VPs\n-- etc.\n```\n\n### Interview Answer\n\n> \"CTEs are temporary named result sets defined with the WITH keyword. They make complex queries readable by breaking them into logical steps. I chain multiple CTEs for multi-step transformations. Recursive CTEs are powerful for hierarchical data like org charts or category trees - the base case selects the root, and the recursive part joins back to find children at each level.\"",
  "### Theory\n\nWindow functions perform calculations across a SET of rows related to the current row - WITHOUT collapsing them into one row like GROUP BY does.\n\n```sql\n-- GROUP BY collapses: 10 rows -> 3 department groups\nSELECT Department, AVG(Salary) FROM Employees GROUP BY Department;\n\n-- WINDOW FUNCTION preserves: 10 rows -> still 10 rows, each with the dept average\nSELECT Name, Department, Salary,\n       AVG(Salary) OVER (PARTITION BY Department) AS DeptAvg\nFROM Employees;\n-- Every employee row is kept, but each one also shows their department's average\n```\n\n### ROW_NUMBER, RANK, DENSE_RANK\n\n```sql\n-- Sample: Salaries = 1000, 900, 900, 800\n\n-- ROW_NUMBER - unique sequential number, no ties\n-- 1, 2, 3, 4 (the two 900s get different numbers, arbitrarily)\n\n-- RANK - same rank for ties, skips numbers\n-- 1, 2, 2, 4 (both 900s get rank 2, next is 4 - skips 3)\n\n-- DENSE_RANK - same rank for ties, no gap\n-- 1, 2, 2, 3 (both 900s get rank 2, next is 3 - no skip)\n\nSELECT Name, Salary,\n    ROW_NUMBER() OVER (ORDER BY Salary DESC) AS RowNum,\n    RANK()       OVER (ORDER BY Salary DESC) AS Rnk,\n    DENSE_RANK() OVER (ORDER BY Salary DESC) AS DenseRnk\nFROM Employees;\n```\n\n### LEAD and LAG - Peek at other rows\n\n```sql\n-- LAG: look at the PREVIOUS row\n-- LEAD: look at the NEXT row\n\nSELECT\n    OrderDate,\n    Amount,\n    LAG(Amount, 1)  OVER (ORDER BY OrderDate) AS PreviousAmount,\n    LEAD(Amount, 1) OVER (ORDER BY OrderDate) AS NextAmount,\n    Amount - LAG(Amount, 1) OVER (ORDER BY OrderDate) AS DayOverDayChange\nFROM DailySales;\n\n-- Result:\n-- 2024-01-01 | 500 | NULL | 600  | NULL\n-- 2024-01-02 | 600 | 500  | 450  | +100\n-- 2024-01-03 | 450 | 600  | 700  | -150\n-- 2024-01-04 | 700 | 450  | NULL | +250\n```\n\n### Other Useful Window Functions\n\n```sql\nSELECT Name, Department, Salary,\n    SUM(Salary) OVER (PARTITION BY Department)                        AS DeptTotal,\n    SUM(Salary) OVER (PARTITION BY Department ORDER BY Salary)        AS RunningTotal,\n    COUNT(*)    OVER (PARTITION BY Department)                        AS DeptCount,\n    NTILE(4)    OVER (ORDER BY Salary DESC)                          AS Quartile\nFROM Employees;\n```\n\n### Interview Answer\n\n> \"Window functions compute values across related rows without collapsing them like GROUP BY. ROW_NUMBER assigns unique sequential numbers, RANK allows ties but skips, DENSE_RANK allows ties without skipping. LAG and LEAD access previous and next rows - great for day-over-day comparisons. I use PARTITION BY to scope the calculation to groups. These are essential for rankings, running totals, and time-series analysis.\"",
  "```sql\n-- Method 1: DENSE_RANK (handles ties correctly)\nWITH Ranked AS (\n    SELECT Salary, DENSE_RANK() OVER (ORDER BY Salary DESC) AS Rnk\n    FROM Employees\n)\nSELECT DISTINCT Salary FROM Ranked WHERE Rnk = 2;\n\n-- Method 2: Subquery\nSELECT MAX(Salary) FROM Employees\nWHERE Salary < (SELECT MAX(Salary) FROM Employees);\n\n-- Method 3: OFFSET (Nth highest - change OFFSET for any position)\nSELECT DISTINCT Salary FROM Employees\nORDER BY Salary DESC\nOFFSET 1 ROWS FETCH NEXT 1 ROWS ONLY;\n```\n\n### Interview Answer\n\n> \"I'd use DENSE_RANK - partition by nothing, order by salary descending, then filter where rank equals 2. DENSE_RANK handles ties correctly - if two people share the highest salary, the second distinct salary still gets rank 2. For the Nth highest, just change the filter to rank = N.\"",
  "```sql\n-- FIND duplicates\nSELECT Name, Email, COUNT(*) AS Count\nFROM Customers\nGROUP BY Name, Email\nHAVING COUNT(*) > 1;\n\n-- DELETE duplicates keeping the one with the lowest Id\nWITH Duplicates AS (\n    SELECT Id,\n           ROW_NUMBER() OVER (PARTITION BY Name, Email ORDER BY Id) AS RowNum\n    FROM Customers\n)\nDELETE FROM Duplicates WHERE RowNum > 1;\n-- ROW_NUMBER 1 = the original (kept)\n-- ROW_NUMBER 2, 3, etc. = duplicates (deleted)\n```\n\n### Interview Answer\n\n> \"I use a CTE with ROW_NUMBER partitioned by the columns that define a duplicate. Row 1 in each group is the original - I delete everything where RowNum > 1. The ORDER BY inside ROW_NUMBER determines which one survives - usually the one with the lowest Id or the earliest creation date.\"",
  "```sql\n-- Original data:\n-- | Product | Month | Sales |\n-- | Laptop  | Jan   | 100   |\n-- | Laptop  | Feb   | 150   |\n-- | Phone   | Jan   | 200   |\n-- | Phone   | Feb   | 250   |\n\n-- Goal: One row per product, months as columns\n\n-- Method 1: CASE + GROUP BY (works everywhere)\nSELECT\n    Product,\n    SUM(CASE WHEN Month = 'Jan' THEN Sales ELSE 0 END) AS Jan,\n    SUM(CASE WHEN Month = 'Feb' THEN Sales ELSE 0 END) AS Feb\nFROM Sales\nGROUP BY Product;\n\n-- Result:\n-- | Product | Jan | Feb |\n-- | Laptop  | 100 | 150 |\n-- | Phone   | 200 | 250 |\n\n-- Method 2: PIVOT (SQL Server only)\nSELECT Product, [Jan], [Feb]\nFROM Sales\nPIVOT (SUM(Sales) FOR Month IN ([Jan], [Feb])) AS pvt;\n```\n\n### Interview Answer\n\n> \"I use conditional aggregation with CASE statements inside SUM - it's portable across all databases. SUM(CASE WHEN Month = 'Jan' THEN Sales END) creates each column. SQL Server also has PIVOT syntax which is cleaner but database-specific. For dynamic columns where I don't know the values upfront, I use dynamic SQL to generate the CASE statements.\"",
  "```\nDELETE    -> removes specific rows (with WHERE), logged per row, triggers fire,\n            can be rolled back, table and schema remain, identity NOT reset\n\nTRUNCATE -> removes ALL rows, minimal logging (faster), triggers DON'T fire,\n            can be rolled back (in SQL Server), table and schema remain,\n            identity IS reset to seed\n\nDROP     -> removes the ENTIRE TABLE - data, schema, indexes, constraints,\n            everything. Gone.\n```\n\n```sql\nDELETE FROM Orders WHERE Status = 'Cancelled';   -- removes specific rows\nTRUNCATE TABLE TempOrders;                        -- removes all rows, fast\nDROP TABLE OldArchive;                            -- table ceases to exist\n```\n\n### Interview Answer\n\n> \"DELETE removes specific rows with WHERE, is fully logged, and fires triggers - use for selective removal. TRUNCATE removes all rows with minimal logging and resets identity - use for clearing staging tables. DROP removes the entire table structure. DELETE and TRUNCATE in SQL Server are both rollbackable inside a transaction, but TRUNCATE can't be used on tables referenced by foreign keys.\"",
  "### Theory\n\nA self join is when a table is joined with itself. Used for hierarchical or comparative data.\n\n```sql\n-- Employee-Manager hierarchy\nSELECT\n    e.Name AS Employee,\n    m.Name AS Manager\nFROM Employees e\nLEFT JOIN Employees m ON e.ManagerId = m.Id;\n\n-- Find employees in the same department earning different salaries\nSELECT a.Name, b.Name, a.Department\nFROM Employees a\nJOIN Employees b ON a.Department = b.Department AND a.Id < b.Id\nWHERE a.Salary <> b.Salary;\n```\n\n### Interview Answer\n\n> \"A self join joins a table to itself using different aliases. The classic use case is an employee-manager hierarchy - join the employees table to itself where one alias's ManagerId equals the other's Id. I also use it for comparing rows within the same table, like finding duplicate records or pairs.\"",
  "### Theory\n\nAn index is like a book's index - instead of reading every page to find \"React,\" you look in the index, find the page number, and go directly there.\n\n**Without index:** SQL scans EVERY row in the table (table scan). 10 million rows? Reads all 10 million.\n\n**With index:** SQL looks up the value in a sorted structure (B-tree), finds the location, jumps directly there. Reads maybe 3-4 pages.\n\n### Types\n\n**Clustered Index:**\n- The table data IS physically sorted by this index\n- Like a phone book sorted by last name - the book itself IS the index\n- Only ONE per table (data can only be sorted one way)\n- Primary key creates a clustered index by default\n\n**Non-Clustered Index:**\n- A separate structure that POINTS to the data\n- Like a book's back-of-book index - separate from the content, has page numbers\n- Many per table (you can have multiple indexes)\n\n**Composite Index:**\n- An index on MULTIPLE columns\n```sql\nCREATE INDEX IX_Orders_Customer_Date ON Orders (CustomerId, OrderDate);\n-- Helps: WHERE CustomerId = 5 AND OrderDate > '2024-01-01'\n-- Helps: WHERE CustomerId = 5 (leftmost column)\n-- Does NOT help: WHERE OrderDate > '2024-01-01' (skipping the first column)\n```\nColumn order matters - the leftmost column must be used in the query.\n\n**Covering Index:**\n- An index that contains ALL columns the query needs\n- The query never touches the actual table - everything is in the index\n```sql\nCREATE INDEX IX_Orders_Covering ON Orders (CustomerId) INCLUDE (Amount, Status);\n-- For: SELECT Amount, Status FROM Orders WHERE CustomerId = 5\n-- All data is in the index - no need to look up the table row\n```\n\n### Interview Answer\n\n> \"An index is a sorted data structure that speeds up lookups - like a book's index. A clustered index determines the physical order of data - one per table, usually the primary key. Non-clustered indexes are separate structures with pointers to data - you can have many. Composite indexes cover multiple columns but only help queries that use the leftmost columns. Covering indexes include all columns a query needs, eliminating the table lookup entirely.\"",
  "```\nCLUSTERED                              NON-CLUSTERED\n\nTable data IS sorted by it          ->  Separate structure pointing to data\nOnly ONE per table                  ->  Many per table\nFaster for range queries            ->  Faster for specific lookups\nIS the table (no separate storage)  ->  Separate storage (extra disk space)\nLike a phone book                   ->  Like a book's index page\n```\n\n```sql\n-- Clustered (usually primary key)\nCREATE CLUSTERED INDEX IX_Orders_Id ON Orders (Id);\n-- The rows in the table are physically ordered by Id\n\n-- Non-clustered\nCREATE NONCLUSTERED INDEX IX_Orders_CustomerId ON Orders (CustomerId);\n-- A separate structure maps CustomerId -> row location\n```\n\n### Interview Answer\n\n> \"A clustered index determines the physical storage order - only one per table, like a phone book sorted by name. Non-clustered is a separate lookup structure - many per table, like a book's index. Clustered is better for range scans, non-clustered for point lookups. I choose the clustered index based on the most common range query pattern - usually the primary key or a date column.\"",
  "### Theory\n\nIndexes speed up reads but slow down writes. Every INSERT, UPDATE, DELETE must also update every index on that table.\n\n**Don't index when:**\n- Table is small (< few thousand rows) - full scan is fast enough\n- Column has low cardinality - like a boolean (true/false). Index barely helps.\n- Column is rarely queried in WHERE, JOIN, or ORDER BY\n- Table has heavy write workload - each write updates all indexes\n- Column is frequently updated - index must be rebuilt constantly\n\n**Downsides of over-indexing:**\n- Each index uses disk space\n- INSERT/UPDATE/DELETE become slower (must maintain all indexes)\n- Database optimizer might choose the wrong index\n- Index maintenance (rebuilding, defragmenting) takes more time\n\n### Interview Answer\n\n> \"I don't index low-cardinality columns like booleans - the optimizer would scan anyway. I avoid indexing heavily-written tables excessively because each write updates every index. I index columns used in WHERE, JOIN, and ORDER BY, but I monitor unused indexes and drop them. The sweet spot is indexing what you query and keeping write performance acceptable.\"",
  "### Theory\n\nThe execution plan shows HOW the database will execute your query - what order, which indexes, how it joins, how much data it reads.\n\n**How to read one:**\n```sql\n-- SQL Server\nSET STATISTICS IO ON;\n-- Then look at the graphical execution plan\n\n-- PostgreSQL\nEXPLAIN ANALYZE SELECT * FROM Orders WHERE CustomerId = 5;\n```\n\n**What to look for:**\n\n```\nGOOD signs:\n- Index Seek    - went directly to the rows (used an index)\n- Index Scan    - scanned part of an index (acceptable)\n- Nested Loop   - efficient for small result sets\n\nBAD signs:\n- Table Scan    - read EVERY row in the table (no index used)\n- Key Lookup    - found the row via index but had to go back to table for columns\n                  (fix: create a covering index with INCLUDE)\n- Sort          - sorting in memory (expensive if large)\n- Hash Match    - building a hash table for join (expensive for large datasets)\n- High estimated rows vs actual rows - statistics are stale, run UPDATE STATISTICS\n```\n\n**Optimization steps:**\n1. Look for Table Scans -> add an index\n2. Look for Key Lookups -> make it a covering index\n3. Look for Sort operations -> add ORDER BY column to index\n4. Check estimated vs actual rows -> update statistics\n5. Look for implicit conversions -> fix data type mismatches\n\n### Interview Answer\n\n> \"I use execution plans to find performance bottlenecks. I look for table scans, which mean a missing index. Key lookups tell me to add INCLUDE columns. Sort operations suggest adding the ORDER BY column to an existing index. I also check estimated vs actual row counts - a big mismatch means stale statistics. I always measure before and after to confirm improvements.\"",
  "### Theory\n\nACID is a set of guarantees for database transactions.\n\n**Banking example: Transfer $500 from Alice to Bob**\n\n**A - Atomicity: \"All or nothing\"**\n```sql\nBEGIN TRANSACTION;\n    UPDATE Accounts SET Balance = Balance - 500 WHERE Name = 'Alice';\n    UPDATE Accounts SET Balance = Balance + 500 WHERE Name = 'Bob';\nCOMMIT;\n-- If the second UPDATE fails, the first is ROLLED BACK\n-- You never end up with money deducted from Alice but not added to Bob\n```\n\n**C - Consistency: \"Valid state before and after\"**\n```\nBefore: Alice = 1000, Bob = 500, Total = 1500\nAfter:  Alice = 500,  Bob = 1000, Total = 1500\n-- Total is still 1500 - rules are preserved\n-- If a constraint is violated, the transaction is rejected\n```\n\n**I - Isolation: \"Transactions don't interfere with each other\"**\n```\nTransaction A is transferring money\nTransaction B tries to read Alice's balance at the same time\nIsolation determines what B sees - the old balance? The new one? Blocked until A finishes?\n```\n\n**D - Durability: \"Once committed, it's permanent\"**\n```\nAfter COMMIT, even if the server crashes 1 millisecond later,\nthe transaction is NOT lost - it's written to disk/WAL\n```\n\n### Interview Answer\n\n> \"ACID ensures reliable transactions. Atomicity means all operations succeed or all roll back - no half-transfers. Consistency means the database moves from one valid state to another - constraints are never violated. Isolation controls how concurrent transactions see each other's changes. Durability means committed data survives crashes. I configure isolation levels based on the trade-off between correctness and performance.\"",
  "### Theory\n\nEach level offers different trade-offs between correctness and performance.\n\n```\nLEVEL                    DIRTY READ?   NON-REPEATABLE?   PHANTOM?   SPEED\n\nRead Uncommitted         Yes           Yes                Yes        Fastest\nRead Committed           No            Yes                Yes        Fast\nRepeatable Read          No            No                 Yes        Slower\nSerializable             No            No                 No         Slowest\n```\n\n**Read Uncommitted:** \"I can see other transactions' uncommitted changes.\" Dangerous but fast. Used for rough reporting where accuracy doesn't matter.\n\n**Read Committed:** \"I only see committed data.\" Default in most databases. But if I read a row, then someone else changes it and commits, my second read sees the new value.\n\n**Repeatable Read:** \"Once I read a row, it stays the same for my entire transaction.\" But new rows matching my criteria can still appear (phantoms).\n\n**Serializable:** \"Transactions execute as if they were serial - one at a time.\" Safest but slowest. Can cause deadlocks.\n\n### Interview Answer\n\n> \"Read Uncommitted is fastest but allows dirty reads - I'd use it for dashboard approximations. Read Committed is the default - prevents dirty reads but a row can change between two reads in the same transaction. Repeatable Read locks rows I've read so they can't change. Serializable is the strictest - prevents phantoms too but at the cost of performance and deadlock risk. I usually stick with Read Committed and use optimistic concurrency where needed.\"",
  "### Theory\n\nA deadlock occurs when two transactions are each waiting for the other to release a lock. Neither can proceed.\n\n```\nTransaction A: locks Row 1, wants Row 2\nTransaction B: locks Row 2, wants Row 1\n\nA is waiting for B to release Row 2\nB is waiting for A to release Row 1\nBoth wait forever -> DEADLOCK\n```\n\n**Detection:** The database detects deadlocks automatically and kills one transaction (the \"victim\"), rolling it back.\n\n**Prevention:**\n```sql\n-- 1. Always access tables/rows in the same order\n-- If every transaction locks Row 1 before Row 2, no circular wait\n\n-- 2. Keep transactions short - less time holding locks\n\n-- 3. Use lower isolation levels where possible\n\n-- 4. Use NOLOCK hint for read-only queries (dangerous but avoids locks)\nSELECT * FROM Orders WITH (NOLOCK);\n\n-- 5. Use optimistic concurrency instead of locks\n```\n\n### Interview Answer\n\n> \"A deadlock happens when two transactions each hold a lock the other needs. The database detects this and kills one as the victim. I prevent deadlocks by accessing resources in a consistent order across all transactions, keeping transactions short, and using optimistic concurrency where possible instead of pessimistic locks.\"",
  "### Theory\n\n**Pessimistic:** \"I'm going to lock this row. Nobody can touch it until I'm done.\"\n\n```sql\nBEGIN TRANSACTION;\nSELECT * FROM Products WHERE Id = 1 WITH (UPDLOCK, ROWLOCK);\n-- Row is LOCKED - nobody else can read or write it\nUPDATE Products SET Stock = Stock - 1 WHERE Id = 1;\nCOMMIT;\n-- Lock released\n```\n\n**Optimistic:** \"I'll just try. If someone else changed it, I'll detect and handle the conflict.\"\n\n```sql\n-- Read the row with its version\nSELECT Id, Stock, RowVersion FROM Products WHERE Id = 1;\n-- Stock = 10, RowVersion = 0x0001\n\n-- Later, update with version check\nUPDATE Products SET Stock = 9\nWHERE Id = 1 AND RowVersion = 0x0001;\n-- If RowVersion changed (someone else modified it), 0 rows affected -> conflict!\n\n-- In EF Core:\n-- [Timestamp] public byte[] RowVersion { get; set; }\n-- Throws DbUpdateConcurrencyException on conflict\n```\n\n```\nPessimistic -> locks the row, blocks others, safe but slow\n              Use for: financial transactions, inventory with high contention\n\nOptimistic  -> no locks, detects conflicts on save, fast but needs retry logic\n              Use for: web apps, forms, low-contention scenarios\n```\n\n### Interview Answer\n\n> \"Pessimistic locking acquires a lock upfront - safe but blocks other transactions. I use it for financial operations where two concurrent updates could cause real damage. Optimistic locking uses a version column - no locks, but on save it checks if the version changed. If it did, someone else modified the row and I handle the conflict. I use optimistic for web APIs where users rarely edit the same record simultaneously.\"",
  "### Theory\n\n**Dirty Read:** Reading data that another transaction hasn't committed yet. If they roll back, you read data that never existed.\n\n```\nTransaction A: UPDATE Balance SET Amount = 0 WHERE UserId = 1  (not committed yet)\nTransaction B: SELECT Amount FROM Balance WHERE UserId = 1  -> reads 0\nTransaction A: ROLLBACK  -> Amount goes back to 1000\nTransaction B used 0 - which was never real\n```\n\n**Non-Repeatable Read:** Reading the same row twice in one transaction and getting different values because someone else changed and committed it in between.\n\n```\nTransaction A: SELECT Amount FROM Balance WHERE UserId = 1  -> 1000\nTransaction B: UPDATE Amount = 500 WHERE UserId = 1; COMMIT\nTransaction A: SELECT Amount FROM Balance WHERE UserId = 1  -> 500\nSame query, different result within the same transaction\n```\n\n**Phantom Read:** Running the same query twice and getting different ROWS because someone else inserted or deleted rows in between.\n\n```\nTransaction A: SELECT * FROM Orders WHERE Amount > 100  -> 10 rows\nTransaction B: INSERT INTO Orders (Amount) VALUES (500); COMMIT\nTransaction A: SELECT * FROM Orders WHERE Amount > 100  -> 11 rows\nA new \"phantom\" row appeared\n```\n\n### Interview Answer\n\n> \"Dirty reads see uncommitted changes that might roll back - prevented at Read Committed level. Non-repeatable reads see different values for the same row within one transaction - prevented at Repeatable Read. Phantom reads see new rows appearing in the same query - prevented at Serializable. Each higher isolation level prevents more anomalies but reduces concurrency.\"",
  "### Theory\n\nNormalization eliminates data redundancy by organizing columns and tables.\n\n**1NF - No repeating groups, each cell has one value**\n```\nVIOLATES 1NF:\n| Student | Subjects          |\n| Alice   | Math, Science     |  <- multiple values in one cell\n\n1NF FIX:\n| Student | Subject |\n| Alice   | Math    |\n| Alice   | Science |\n```\n\n**2NF - 1NF + no partial dependencies (every non-key column depends on the ENTIRE primary key)**\n```\nVIOLATES 2NF (composite key: StudentId + CourseId):\n| StudentId | CourseId | CourseName | Grade |\n  CourseName depends only on CourseId, not on the full key\n\n2NF FIX - separate table:\nEnrollments: | StudentId | CourseId | Grade |\nCourses:     | CourseId  | CourseName |\n```\n\n**3NF - 2NF + no transitive dependencies (non-key column depending on another non-key column)**\n```\nVIOLATES 3NF:\n| EmployeeId | DepartmentId | DepartmentName |\n  DepartmentName depends on DepartmentId, not on EmployeeId\n\n3NF FIX:\nEmployees:   | EmployeeId | DepartmentId |\nDepartments: | DepartmentId | DepartmentName |\n```\n\n**BCNF (Boyce-Codd) - Every determinant is a candidate key**\nStricter than 3NF. Rarely comes up in practice unless you have overlapping candidate keys.\n\n### Interview Answer\n\n> \"1NF eliminates repeating groups - each cell holds one value. 2NF removes partial dependencies - non-key columns must depend on the entire composite key. 3NF removes transitive dependencies - non-key columns can't depend on other non-key columns. In practice, I normalize to 3NF for transactional systems and denormalize selectively for read-heavy reporting.\"",
  "### Theory\n\nIntentionally adding redundancy back to improve read performance.\n\n```sql\n-- NORMALIZED: To get an order with customer name, you JOIN\nSELECT o.Id, c.Name, o.Amount\nFROM Orders o JOIN Customers c ON o.CustomerId = c.Id;\n\n-- DENORMALIZED: Customer name stored directly in Orders\nSELECT Id, CustomerName, Amount FROM Orders;\n-- No JOIN needed - faster reads\n-- But if customer changes name, you update it in two places\n```\n\n**When justified:**\n- Read-heavy reporting/analytics where JOINs are expensive\n- Denormalized read models in CQRS\n- Caching layers (Redis stores denormalized data)\n- NoSQL databases (Cosmos DB, MongoDB) where JOINs are impossible or expensive\n- Pre-computed aggregates for dashboards\n\n### Interview Answer\n\n> \"Denormalization intentionally adds redundancy for read performance. I use it for reporting tables, dashboard pre-aggregations, and NoSQL data models where JOINs are expensive. The trade-off is write complexity - updates must maintain consistency across duplicated data. I keep the transactional system normalized and denormalize the read side.\"",
  "### Theory\n\n```\nSTORED PROCEDURE                      FUNCTION\n\nCan modify data (INSERT/UPDATE)    ->  Should only READ data\nReturns 0 or multiple result sets  ->  Returns a single value or table\nCalled with EXEC                   ->  Called inside a query (SELECT, WHERE)\nCan have output parameters         ->  Must return a value\nCan use transactions               ->  Cannot (usually)\nCan call functions                 ->  Cannot call procedures\n```\n\n```sql\n-- STORED PROCEDURE - does work\nCREATE PROCEDURE PlaceOrder @CustomerId INT, @Amount DECIMAL\nAS\nBEGIN\n    INSERT INTO Orders (CustomerId, Amount, Status) VALUES (@CustomerId, @Amount, 'New');\n    UPDATE Customers SET LastOrderDate = GETDATE() WHERE Id = @CustomerId;\nEND;\nEXEC PlaceOrder @CustomerId = 1, @Amount = 500;\n\n-- FUNCTION - returns a value, used in queries\nCREATE FUNCTION GetCustomerTotal(@CustomerId INT)\nRETURNS DECIMAL\nAS\nBEGIN\n    DECLARE @Total DECIMAL;\n    SELECT @Total = SUM(Amount) FROM Orders WHERE CustomerId = @CustomerId;\n    RETURN @Total;\nEND;\nSELECT Name, dbo.GetCustomerTotal(Id) AS TotalSpent FROM Customers;\n```\n\n### Interview Answer\n\n> \"Stored procedures can modify data and handle complex business logic with transactions - I use them for batch operations and migration scripts. Functions return values and can be used inside SELECT or WHERE clauses - I use them for reusable calculations. In most modern projects, I keep business logic in the application layer and use stored procedures mainly for performance-critical bulk operations.\"",
  "### Theory\n\nA trigger is code that automatically runs when a specific event happens (INSERT, UPDATE, DELETE).\n\n```sql\n-- Audit trigger: log every change to Orders\nCREATE TRIGGER trg_Orders_Audit\nON Orders\nAFTER UPDATE\nAS\nBEGIN\n    INSERT INTO OrderAudit (OrderId, OldAmount, NewAmount, ChangedAt)\n    SELECT i.Id, d.Amount, i.Amount, GETDATE()\n    FROM inserted i       -- \"inserted\" = new values\n    JOIN deleted d ON i.Id = d.Id;  -- \"deleted\" = old values\nEND;\n```\n\n**When they're dangerous:**\n- Hidden side effects - someone inserts a row and has no idea a trigger fires\n- Performance - triggers that do complex work slow down every write\n- Cascading triggers - trigger A fires trigger B fires trigger C\n- Hard to debug - not visible in the calling code\n- Hard to test - logic buried in the database\n\n### Interview Answer\n\n> \"Triggers run automatically on INSERT/UPDATE/DELETE. I use them cautiously - mainly for audit logging where I need guaranteed capture regardless of which application writes to the table. They're dangerous because they're invisible to the caller, can cascade unpredictably, and bury logic in the database. For most business logic, I prefer application-level code that's testable and visible.\"",
  "### Theory\n\n**View** - A saved query. No data stored. Runs the query every time you select from it.\n\n```sql\nCREATE VIEW ActiveCustomerOrders AS\nSELECT c.Name, o.Amount, o.OrderDate\nFROM Customers c\nJOIN Orders o ON c.Id = o.CustomerId\nWHERE c.IsActive = 1;\n\nSELECT * FROM ActiveCustomerOrders;\n-- Every time you query this, it runs the JOIN fresh\n```\n\n**Materialized View** - A saved query with CACHED results. Data is stored physically. Must be refreshed to stay current.\n\n```sql\n-- SQL Server uses \"Indexed View\"\n-- PostgreSQL uses MATERIALIZED VIEW\nCREATE MATERIALIZED VIEW MonthlySales AS\nSELECT\n    DATE_TRUNC('month', OrderDate) AS Month,\n    SUM(Amount) AS Total\nFROM Orders GROUP BY DATE_TRUNC('month', OrderDate);\n\nREFRESH MATERIALIZED VIEW MonthlySales;\n-- Results are precomputed and stored - reads are instant\n-- But data is stale until you refresh\n```\n\n```\nView               -> always fresh, no storage, can be slow (runs query each time)\nMaterialized View  -> fast reads, uses storage, can be stale (needs refresh)\n```\n\n### Interview Answer\n\n> \"A view is a saved query that runs fresh every time - no storage, always current. A materialized view stores the results physically - fast reads but can be stale. I use regular views for access control and query simplification. Materialized views for expensive aggregations that don't need real-time accuracy, like dashboards and reports.\"",
  "### Theory\n\nThree approaches:\n\n**1. Shared database, shared schema (column-based isolation)**\n```sql\nCREATE TABLE Orders (\n    Id INT, TenantId INT, Amount DECIMAL, ...\n);\n-- Every query includes: WHERE TenantId = @CurrentTenant\n-- Pro: simplest, cheapest\n-- Con: risk of data leaks if you forget WHERE, noisy neighbor\n```\n\n**2. Shared database, separate schemas**\n```sql\n-- Each tenant gets their own schema\nCREATE SCHEMA tenant_acme;\nCREATE TABLE tenant_acme.Orders (...);\n\nCREATE SCHEMA tenant_globex;\nCREATE TABLE tenant_globex.Orders (...);\n-- Pro: better isolation, easier per-tenant backup\n-- Con: schema management complexity, migrations run per tenant\n```\n\n**3. Separate databases**\n```\ntenant_acme_db -> has its own Orders, Customers, etc.\ntenant_globex_db -> completely separate\n-- Pro: strongest isolation, per-tenant scaling, compliance\n-- Con: most expensive, hardest to manage\n```\n\n**My recommendation for most SaaS:** Shared schema with TenantId + Row-Level Security.\n\n```sql\n-- Row-Level Security (SQL Server) - database enforces tenant isolation\nCREATE SECURITY POLICY TenantFilter\nADD FILTER PREDICATE dbo.fn_TenantFilter(TenantId) ON dbo.Orders;\n-- Even if code forgets WHERE TenantId = X, the policy blocks cross-tenant access\n```\n\n### Interview Answer\n\n> \"Three approaches: shared schema with TenantId column (simplest, cheapest), separate schemas (better isolation), or separate databases (strongest isolation, most expensive). I'd start with shared schema using Row-Level Security to enforce tenant isolation at the database level - so even a bug in application code can't leak data across tenants. I'd move to separate databases only for compliance requirements or tenants that need dedicated resources.\"",
  "### Theory\n\nInstead of actually deleting a row, mark it as deleted. The data stays for auditing and recovery.\n\n```sql\nALTER TABLE Customers ADD IsDeleted BIT DEFAULT 0;\nALTER TABLE Customers ADD DeletedAt DATETIME NULL;\nALTER TABLE Customers ADD DeletedBy INT NULL;\n\n-- \"Delete\" a customer\nUPDATE Customers\nSET IsDeleted = 1, DeletedAt = GETDATE(), DeletedBy = @UserId\nWHERE Id = @CustomerId;\n\n-- All queries must filter out deleted records\nSELECT * FROM Customers WHERE IsDeleted = 0;\n\n-- Better: use a default-scoped view\nCREATE VIEW ActiveCustomers AS\nSELECT * FROM Customers WHERE IsDeleted = 0;\n```\n\n**In EF Core:**\n```csharp\n// Global query filter - automatically applies WHERE IsDeleted = 0\nmodelBuilder.Entity<Customer>().HasQueryFilter(c => !c.IsDeleted);\n\n// To see deleted records:\ndb.Customers.IgnoreQueryFilters().Where(c => c.IsDeleted).ToList();\n```\n\n### Interview Answer\n\n> \"Soft delete marks records as deleted instead of removing them - I add IsDeleted, DeletedAt, and DeletedBy columns. In EF Core, I use global query filters so all queries automatically exclude soft-deleted records. This preserves audit trails and enables recovery. The downside is table bloat over time - I archive soft-deleted records older than the retention period to a separate table.\"",
  "```sql\nSELECT\n    OrderDate,\n    DailyTotal,\n    AVG(DailyTotal) OVER (\n        ORDER BY OrderDate\n        ROWS BETWEEN 6 PRECEDING AND CURRENT ROW\n    ) AS Rolling7DayAvg\nFROM (\n    SELECT OrderDate, SUM(Amount) AS DailyTotal\n    FROM Orders\n    GROUP BY OrderDate\n) DailySales\nORDER BY OrderDate;\n\n-- For each day, averages the current day + previous 6 days\n-- Day 7: avg of days 1-7\n-- Day 8: avg of days 2-8\n-- Day 9: avg of days 3-9\n```\n\n### Interview Answer\n\n> \"I use a window function with ROWS BETWEEN 6 PRECEDING AND CURRENT ROW to compute a rolling average. The inner query aggregates daily totals, and the window function averages the current day plus the six preceding days. This is a common pattern for time-series analysis and dashboards.\"",
  "```sql\nSELECT\n    e.Name AS Employee,\n    e.Salary AS EmployeeSalary,\n    m.Name AS Manager,\n    m.Salary AS ManagerSalary\nFROM Employees e\nJOIN Employees m ON e.ManagerId = m.Id\nWHERE e.Salary > m.Salary;\n```\n\nThis is a self-join - the table is used twice with different aliases.\n\n### Interview Answer\n\n> \"Self-join with an alias for each role. Join employees to employees where ManagerId equals Id, then filter where the employee's salary exceeds the manager's. It's a classic self-join pattern.\"",
  "### Theory\n\nBoth split data into smaller pieces, but at different levels.\n\n**Partitioning** - Splitting data within ONE database. The database manages it.\n\n```sql\n-- Partition orders by year - all in the same database\nCREATE PARTITION FUNCTION pf_OrderDate (DATE)\nAS RANGE RIGHT FOR VALUES ('2022-01-01', '2023-01-01', '2024-01-01');\n\n-- Queries automatically scan only relevant partitions\nSELECT * FROM Orders WHERE OrderDate >= '2024-01-01';\n-- Only touches the 2024 partition, skips 2022 and 2023\n```\n\n**Sharding** - Splitting data across MULTIPLE databases/servers. The application manages it.\n\n```\nShard 1 (Server A): Customers A-M\nShard 2 (Server B): Customers N-Z\n\nApplication decides: \"Customer 'Alice' -> Shard 1\"\n                     \"Customer 'Noah' -> Shard 2\"\n```\n\n```\nPARTITIONING                        SHARDING\n\nWithin one database             ->   Across multiple databases/servers\nDatabase handles routing        ->   Application handles routing\nEasier to implement             ->   Complex to implement\nLimited by one server           ->   Scales horizontally (add servers)\nTransparent to the application  ->   Application must know which shard\n```\n\n### Interview Answer\n\n> \"Partitioning splits data within one database - typically by date range or region. The database handles routing transparently. Sharding splits data across multiple servers - the application routes queries based on a shard key. Partitioning is simpler but limited by one server. Sharding enables horizontal scaling but adds complexity - cross-shard queries are expensive, and rebalancing when adding shards is non-trivial.\"",
  "### Theory\n\n```\nFEATURE              SQL SERVER          POSTGRESQL          MYSQL\n\nLicense            -> Commercial          Open source         Open source (Oracle)\nACID               -> Full                Full                Full (InnoDB)\nJSON support       -> Good (OPENJSON)     Excellent (JSONB)   Good (JSON type)\nFull-text search   -> Built-in            Built-in            Built-in\nWindow functions   -> Full support        Full support        Since 8.0\nCTEs               -> Full + recursive    Full + recursive    Since 8.0\nPartitioning       -> Table partitioning  Declarative         Range/Hash/List\nReplication        -> Always On AG        Streaming + logical Various\nStored procedures  -> T-SQL               PL/pgSQL            SQL/PSM\nExtensions         -> CLR integration     Rich ecosystem      Limited\n                                         (PostGIS, etc.)\nMVCC               -> Snapshot isolation  Native MVCC         InnoDB MVCC\nCost               -> $$$ (free Express)  Free                Free\n\nBEST FOR:\nSQL Server -> Enterprise .NET apps, Microsoft ecosystem, BI with SSRS/SSIS\nPostgreSQL -> Complex queries, GIS data, JSON-heavy, open-source preference\nMySQL      -> Web apps, WordPress, simple CRUD, read-heavy workloads\n```\n\n### Interview Answer\n\n> \"SQL Server integrates deeply with the Microsoft ecosystem - great with .NET, has excellent tooling with SSMS, and strong BI capabilities. PostgreSQL is the most feature-rich open-source option - superior JSON support with JSONB, excellent extensibility with PostGIS for spatial data, and strict standards compliance. MySQL is simpler and traditionally faster for basic read-heavy workloads.\n>\n> I've primarily used SQL Server with .NET projects and Cosmos DB for document storage. My choice depends on the ecosystem - SQL Server for .NET shops, PostgreSQL for open-source stacks, MySQL for simple web applications.\""
];
